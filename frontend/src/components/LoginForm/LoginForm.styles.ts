import styled from "styled-components";

export const Form = styled.form`
  display: flex;
  flex-direction: column;
  padding: 0.5rem;
  color: white;
  border-radius: 10%;
  background: rgba(255, 255, 255, 0.65);
  box-shadow: 0 3px 13px 13px rgb(0 0 0 / 4%);
`;
